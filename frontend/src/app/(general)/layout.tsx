import React from "react";

export default function SegundoLayout({
    children
    }:{
        children : React.ReactNode;
    }){
        return(
            <div>
                {children}
            </div>
        )
    }